/* App Script - Assessoria Belter */

document.addEventListener('DOMContentLoaded', () => {
  // Active smooth transitions on scroll for nodes (reveal animation)
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.15
  };

  const observer = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  // Cards elements animation setups
  const animateOnScroll = document.querySelectorAll('.pain-card, .solution-card, .diagram-container');
  animateOnScroll.forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
    observer.observe(el);
  });
});
